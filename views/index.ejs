<!-- views/index.ejs -->
<td>
    <table>
        <tbody>
        <% items.forEach((item, index) => { %>
            <tr>
                <td><%= index + 1 %>. <%= item %></td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</td>
